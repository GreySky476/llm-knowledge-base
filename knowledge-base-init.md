你是一个 AI 开发团队的“架构师与知识库规划者”。你的任务是重新规划当前项目的知识库，使其能够支持“需求理解 → 执行 → 测试 → 迭代”的全量 AI 开发闭环。

你需要从项目现状出发，生成一整套可被后续 AI 智能体依赖的、结构化、可维护的知识库。

请严格按照以下阶段执行，每完成一个阶段都向我确认后，再进行下一阶段。

---

## 阶段一：项目扫描与理解
1. 阅读项目根目录下的所有配置文件（如 package.json, tsconfig.json, Cargo.toml, requirements.txt 等），识别技术栈、主要依赖、脚本命令，若不存在阅读 doc/ 下文档和 AGENTS.md 搞清楚该项目是干什么的。
2. 阅读项目的目录结构，识别出核心模块、入口文件、测试目录、API 层、数据层、组件库等。
3. 如果存在旧的 AGENTS.md，先阅读它，提取其中仍然有效的规则、约束和架构描述。
4. 扫描关键源文件（如路由定义、数据模型、核心业务逻辑），概括出项目的领域模型和主要功能模块。
5. 扫描现有测试文件，了解测试框架和用例覆盖范围。

请输出一份**项目现状报告**，如果项目有方案，按照方案规划项目结构，至少包含：
- 技术栈
- 核心模块列表与职责
- 入口点与运行方式
- 主要数据实体
- 测试情况

---

## 阶段二：重建 AGENTS.md (AI 索引地图)
基于阶段一的报告，重新生成一份 `AGENTS.md`，作为所有 AI 智能体的入口文件。
要求：
- 它必须是“指针式”的，引导智能体去特定子文档查找详细信息，而不是把一切写在这里。
- 长度控制在 100 行以内。
- 必须包含以下部分：
  - 🧭 快速定位：给出各类信息对应的文档路径（架构、模块、API、Schema、约定、Roadmap、任务等）。
  - 🤖 可用工具与命令：列出开发、测试、构建、Lint、启动环境等命令。
  - ⚠️ 铁律：列出 AI 智能体必须绝对遵守的规则（如不能直接操作主分支、必须先读某个文件等），优先级最高。
  - 🔄 工作流提示：简要说明标准开发流程中的步骤（例如：领取任务 → 阅读相关文档 → 实现 → 自检 → 更新文档）。

完成后生成新的 `AGENTS.md` 文件内容，并等待我确认。

---

## 阶段三：建立分层知识库
我需要你产出以下一系列文件，构成完整的知识库。每份文件都要基于现有项目实际情况生成，而不是空模板。

### 1. `docs/conventions.md` – 开发规范与约束
提炼出项目中已经存在（或应当存在）的编码规范、命名约定、API 设计风格、错误处理模式、Git 提交规范、组件状态覆盖要求（Loading/Empty/Error）等。如果项目中缺少明确规范，请基于业界最佳实践和当前代码风格推断并补充，并明确标注哪些是“从现有代码提取”，哪些是“建议新增”。

### 2. `docs/architecture.md` – 高层架构设计
描述项目的整体架构模式（如分层架构、微服务、模块化单体等），组件/模块之间的依赖关系，数据流，以及关键技术选型的理由（如果能从代码或旧文档中推断）。

### 3. 模块文档 `docs/modules/{module-name}.md`
为每个核心模块生成一个文档，至少包含：
- 模块职责
- 对外暴露的接口/API
- 依赖的其他模块
- 修改历史表格（初始行标记为当前日期和“初始文档”）

### 4. `docs/schema/` – 数据模型与 Schema
如果项目涉及数据库或外部 API 的结构，请在此目录生成对应文档，描述核心实体、字段、关系以及任何数据结构约定。

### 5. `docs/adr/` – 架构决策记录
从现有代码和配置中回溯识别出关键设计决策，生成至少 1 个 ADR。例如：为什么选择某个框架/库，为什么采用某种认证方式，为什么目录结构如此组织。如果无法从代码推断，可基于现有技术栈创建一个“初始技术选型”决策记录。每个 ADR 使用标准格式：标题、状态、背景、决策、后果。

---

## 阶段四：任务流与维护机制
1. 创建 `docs/roadmap/current.md` 作为当前在做的需求占位文件，初始内容可以为空，但需包含指引：“在此记录当前 Epic/Story 和目标”。
2. 生成一份 `tasks/_template.md` 作为任务卡模板，包含：任务类型（新增/增强）、关联历史、验收标准、受影响模块、设计引用等字段。
3. 在 `AGENTS.md` 或一个单独的 `docs/knowledge-maintenance.md` 中写入知识库维护规则，指导未来的 AI 智能体：
   - 任何功能完成后，必须更新受影响模块文档的“修改历史”。
   - 任何新架构决策必须产出 ADR。
   - 定期检查 `conventions.md` 与代码的一致性（提示可运行某条检测命令）。

---

## 阶段五：最终总结
向我总结生成的所有文件列表及其用途，并给出一个**后续全量 AI 开发的使用指南**，说明在新需求到来时，如何利用这套知识库进行启动、执行、测试、迭代。

请开始执行阶段一。
